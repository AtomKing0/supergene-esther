# 패치필요 — 서버패치 대기 항목

> 완료된 항목은 `_패치완료이력.md` 참조.

---

현재 대기 항목 없음.

## 2026-08-12 · const — SPEC-038 토너 소셜 팝업 토글 스위치 신설
| key_number | const_name | old | new | 효과 |
|---|---|---|---|---|
| **10108** | `tournament_invite_enabled` | (신규) | **1** | 토너 컨텍스트 판 종료 팝업을 공유↔토너 초대(inviteAsync) 토글로 전환하는 온오프. 0=끔(현행 shareTournament만 발화)·1=켬. dev 테스트용 ON 등록. 상수 없거나 파싱 실패 시 0 폴백(안전 기본값) — prod엔 클라 배포 전까지 미적용이라 영향 없음 |

- 스펙: workspace/specs/spec-038-share-exposure-normalization.html
- **값 1(ON)로 등록** — 시트·정본·패치필요 전부 1. `const.json`을 에스더가 dev 서버에 직접 업로드해 QA 테스트
- prod 반영은 클라 정식 배포와 함께(그 전까지 prod 서버엔 이 키가 없어 클라 폴백 0 = 현행 동작). 문제 시 시트·파일 0으로 복귀
