# 그림자 냥이 추격전 — 개발 처리 체크리스트 (핸드오프)

> 2026-07-28 개정 · spec-027 확정분에서 개발 액션만 추출 · 상세 명세 = spec-027-shadow-chase.html
>
> **7-28 변경 요약** — ① 팝업 세션 상한 시 발동 연출 **이월**(유실 금지) ② 결과 화면 **RV ×2 유지**(하단 체스트 블록만 추가) ③ **[광고 보고 +5분]** 타이머 연장 v1 포함 ④ 실패 **토스트 1회** 추가 ⑤ 로그 필드 보강 + `7109` 신설 ⑥ 강제 시청 **6초 상한**

## 1. 발동·사이클 (서버/클라)

- [ ] 이벤트 기간 판정: `event_schedule` 160005 — 월 00:00 ~ 목 23:59 · 매주 반복 (in_use 킬스위치 기구현 활용)
- [ ] 발동 조건 평가(로비 복귀 시): **기간 내 AND 쿨다운 경과 AND 미진행 중** → 충족 시 발동 시퀀스 연출 자동 재생
- [ ] 발동 순번: **기존 로비 팝업 체인(runWinFlow) 마지막 순번**에 추가 — 보상 정산·출석 등 우선 팝업 소화 후
- [ ] 사이클당 1회: 발동 연출은 사이클 시작 시 1회만. 타이머 시작 = 연출 종료 시점
- [ ] ★**세션 상한 소진 시 이월(필수)** — 발동 연출이 팝업 체인 마지막 순번이라 앞 팝업이 세션 상한을 소진하면 큐에서 잘릴 수 있음. **잘라내지 말고 다음 로비 복귀로 이월**할 것. 연출 재생 = 타이머 시작이므로 스킵되면 `7102` 미발화 + **사이클 통째 유실**
- [ ] 이월 중: `cooldown_sec` 타이머 **미시작** · `7101` **재발화 없음**(중복 계상 방지) · 이벤트 기간 이탈 시 보류분 폐기
- [ ] 이월 상태는 enum 추가 없이 **IDLE + 보류 플래그**로 유지 (연출 재생 = `GRAB` 진입)
- [ ] 사이클 종료(성공/실패 무관) → `cooldown_sec`(43200) 재트리거 대기. cooldown_until 서버 저장
- [ ] 타이머 권위 = 서버 시간(wall-clock). 재접속 시 remaining 재계산·복원. 기간 종료를 넘겨도 진행 중 유저는 개인 타이머까지 완주 인정(`grace_after_window`)
- [ ] ★개발 확인 ③: 기간·쿨다운 판정 주체 — 서버 스케줄 push vs 클라 시각 판정(서버 시간 기준) 중 택

## 2. 연출·입력 (클라)

- [ ] 발동 시퀀스(발견→열쇠 강탈→도주→버튼 🐾마킹) 재생 중 **풀스크린 입력 잠금** — `ViewBase.setLocked()`/`lockPopupInput` 재활용
- [ ] 스킵 정책: 최초 1회 필수 시청 · 재노출(다음 사이클)부터 탭 스킵 허용
- [ ] ★**강제 시청 6초 이내**(강탈~도주~마킹 합산 기준) — 원본 CCS는 11초/161프레임이나 우리는 로비 팝업 9개 소화 직후에 얹히는 자리라 이탈 리스크가 큼. 6초 초과 시 **필수 시청은 ②강탈까지만, ④도주부터 탭 스킵 허용**으로 완화
- [ ] `7102`에 `fx_completed`·`fx_watch_sec` 기록 — 연출 중 이탈 측정용(재생 시작만으로는 측정 불가)
- [ ] 캐릭터 본체 미등장 — 🐾자국 스프라이트+Tween만 (도주=발자국 트레일+열쇠 아이콘 이동)
- [ ] 중간 앵커 연출 **없음** (제작 안 함)

## 3. 진행 중 상태 표시 (클라)

- [ ] 프리레벨 행잉 태그(보라·5번째 자리): 체스트+타이머+n/8 게이지(끝에 열쇠 골 마커)+그림자 냥이 실루엣(귀 끝+열쇠 든 발만)
- [ ] 플레이 버튼 🐾마킹(보라 오라+별빛 발자국) 상시 표시
- [ ] ★인게임 HUD·타이머 위젯 **없음** — 인게임 중 이벤트 표시 일절 없음. 타이머·n/8 인지는 매판 사이 프리레벨 행잉 태그에서만
- [ ] 로비 이벤트 아이콘·레드닷 **만들지 않음**
- [ ] 인포 팝업(신규 1종): 프리레벨 행잉 태그·결과 화면 행잉 상자에 **ⓘ 버튼** → 탭 시 이벤트 안내 팝업(서사·목표·체스트 보상 미리보기·남은 시간). 보상 수치는 event_shadow_chest 시트 연동(하드코딩 금지)

## 4. 성공 처리 — 완주 판 결과 화면 (클라/서버)

> ★**7-28 변경** — 기존안(RV ×2를 끄고 [받기] 단일)에서 **영역 분리 방식으로 전환**. 판당 RV가 본 이벤트의 핵심 근거인 만큼 일반 판 보상의 RV ×2는 유지함. **기존 결과 화면을 수정하지 않고 블록만 얹는** 구조라 공수도 줄어듦.

- [ ] **신규 팝업·모달 없음** — 지급 연출은 결과 화면 안 3컷으로 처리(연출 자체는 신규 제작분). 완주 판의 결과 화면 **하단에 신규 체스트 블록** 추가(종류=골드·해머·와일드카드·되돌리기·파이어웍스·무료 입장 티켓 — 수량은 event_shadow_chest 시트)
- [ ] ★**상단 = 기존 판 보상 블록** — RV ×2 버튼 **기존 그대로 유지·로직 무변경**(끄지 말 것)
- [ ] ★**하단 = 신규 체스트 블록** — 🐾마킹 + **[받기] 단일** + 고정 문구 `T_SHADOW_CHASE_CHEST_NO_MULTIPLY`("이벤트 보상은 배수가 적용되지 않아요")
- [ ] 체스트 지급분은 **RV 배수 미적용** — 배수 대상은 상단 판 보상만
- [ ] 연출: 행잉 태그 만충 글로우 → 체스트 팝+파티클이 보상 아이콘으로 → 태그 소멸·체스트 블록 🐾뱃지 (spec ⑦ 3컷 참고)
- [ ] 지급: 서버 커밋(멱등 txn_id) — `/api/event/shadow-chase/claim-chest` 신규 (기존 /api/event/* 관례)

## 4-B. 타이머 연장 — [광고 보고 +5분] (클라/서버) · v1 포함

- [ ] 잔여 시간 ≤ `ad_extend_threshold_sec`(300) AND 미완주 → **프리레벨 행잉 태그 안에 [광고 보고 +5분] 버튼** 노출 (신규 모달 없음)
- [ ] 시청 완료 시 `ad_extend_sec`(300)만큼 개인 타이머 연장 · **사이클당 `ad_extend_max_per_cycle`(1)회** — 서버 검증
- [ ] 연출 제작분 없음 — 타이머 숫자 갱신 정도. 미시청 시 그대로 만료(패널티 없음)
- [ ] 연장 시 상태는 `RUNNING` 유지 (상태 enum 추가 없음)
- [ ] `ad_extend_max_per_cycle=0`이면 기능 OFF (개별 킬스위치)
- [ ] `7109_SHADOW_CHASE_AD_EXTEND` 로깅(노출/시청/결과) · `7105`·`7106`에 `extended(bool)` 기록

## 5. 실패 처리 (클라)

- [ ] **신규 연출·팝업 없음** — 행잉 태그·플레이 버튼 마킹 **비노출 전환**
- [ ] ★**다음 로비 복귀 시 토스트 1회** — `T_SHADOW_CHASE_FAIL_FLEE`. 태그가 조용히 사라지기만 하면 유저가 실패를 인지하지 못해 **재도전 동기가 발생하지 않음**
- [ ] 토스트는 **모달이 아님** — 로비 팝업 체인 순번 미점유·세션 상한 계산 제외·비차단 노출·사이클당 1회(중복 방지 플래그)
- [ ] 몰수 없음: 인게임 획득은 평소대로 유지. 체스트만 미지급 — **"몰수 없음"이 토스트 문구로 전달돼야 함**

## 6. 데이터·코드 등록 (클라/서버)

- [ ] 신규 클래스: `PopupEventShadowChase`(PopupBase 문법 — 발동 연출 재생 + ⓘ 인포 팝업 겸용) · 🐾자국 스프라이트/Tween(GRAB·ESCAPE 연출용, 캐릭터 본체 미등장) · 기존 프리레벨 진행이벤트 행잉 태그 확장(체스트+타이머+n/8 게이지+실루엣). 인게임 HUD 위젯 신규 없음
- [ ] `ProtocolTypes.ts`: EventSchedule.type에 `shadow_chase` 추가 · `ShadowChaseInfo` 타입 신설(timer_start_ts·rounds_cleared·cooldown_until·cycle_result)
- [ ] 진행 저장: 서버 `TPlayer.event_infos`에 ShadowChaseInfo
- [ ] 신규 시트 탭 로드: `event_shadow_sprint`(**8변수** — 기존 5 + `ad_extend_threshold_sec`·`ad_extend_sec`·`ad_extend_max_per_cycle`)·`event_shadow_chest`(Normal 6행) — 번들 JSON+balance-sheet 로더 등록
- [ ] `event_shadow_sprint`는 v1 1행 고정이나, **레벨 구간 차등이 필요해지면 `level_min`/`level_max` 컬럼을 가진 다행 구조로 확장**될 수 있음 — 로더를 단일 행 전제로 하드코딩하지 말 것
- [ ] unlock 50031(`content_event_shadow_chase`·level_clear·10) — 별도 튜토리얼 없음(tutorial_guide 미등록). 클라 코드 등록 불필요(데이터 참조)
- [ ] string_code `T_SHADOW_CHASE_*` **10키** 등록 (+`T_SHADOW_CHASE_AD_EXTEND`·`T_SHADOW_CHASE_CHEST_NO_MULTIPLY`)
- [ ] ★개발 확인 ①: goal_type=`complete_sprint_cycle` 서버 처리(2차 저니용 — v1 훅만)
- [ ] ★개발 확인 ②: milestone/sprint의 event_id=0(비귀속) 관례 수용 여부

## 7. 로그 등록 (클라)

`BaseLogManager` LogType enum + `registLogEvent` 2줄씩 · payload string 직렬화:

| 코드 | 발화 시점 |
|---|---|
| 7101_SHADOW_CHASE_SLOT_OPEN | 발동 조건 충족(NOTIFY) — `deferred(bool)`·`fire_side(server/client)` **추가** · 이월 시 재발화 금지 |
| 7102_SHADOW_CHASE_ACTIVATE | 발동 연출 재생(사이클 시작) — `fx_completed(bool)`·`fx_watch_sec`·`deferred_count` **추가** |
| 7103_SHADOW_CHASE_ROUND_CLEAR | 사이클 중 판 클리어 — `round_idx(1~total_rounds)` **하드코딩 금지**, `level_id`·`round_sec` **추가** |
| 7104 | (미사용 — 앵커 제거) |
| 7105_SHADOW_CHASE_CHEST_OPEN | 완주·체스트 지급(txn_id) — `chest_tier(normal/jackpot)`·`reward_keys[]`·`extended(bool)` **추가** |
| 7106_SHADOW_CHASE_FAIL | 타이머 만료(rounds_cleared·reason) — `extended(bool)` **추가** |
| 7107_SHADOW_CHASE_HAMMER_GRANT | 해머 지급 — **`amount` 추가**(balance_after만으로는 지급량 역산 불가) |
| 7108_SHADOW_CHASE_DECO_GATE_UNLOCK | 해머로 데코 게이트 해제 |
| **7109_SHADOW_CHASE_AD_EXTEND** | **신규** — [광고 보고 +5분] `step(impression/watch_start/complete)`·`remain_sec_before`·`extend_sec`·`result` |

### ★ 로그 필드 보강 이유 (전부 KPI 산출 필수 · 사후 소급 불가)

- `7105.chest_tier` — 없으면 Normal/Jackpot 구분 불가 → "데코 진입률" 산출 불능
- `7107.amount` — `balance_after`만으로는 다른 경로 획득분과 섞여 발행량 역산 불가
- `7103.round_idx` — `total_rounds`가 시트 변수(6판 해피데이 운영 레버)인데 `1~8` 고정이면 차등 세팅 시 스키마 파손
- `7103.level_id`/`round_sec` — **레벨 구간별 판당 소요 실측**에 직결
- `7102.fx_completed` — 강제 시청 구간 이탈 측정
- `7101.deferred`/`fire_side` — 참여율(`7102÷7101`) 분모 정의. 이월분이 분모를 부풀리지 않게 분리

### ★ RV 분해 (계측 설계 · v1 필수)

- [ ] `4210_RV_FINISH`(지면 = `coalesce(data.type, data.position)`)를 `7103`의 **사이클 귀속 판**과 조인 → **스프린트 창 안 판 vs 밖 판의 판당 RV 노출·시청률 분해**
- [ ] 이유: 25분 8판 압축은 유저가 **다음 판으로 빨리 가려고 RV를 스킵**할 유인을 키움. 판수가 늘어도 판당 RV가 깎이면 순증분이 상쇄됨. **사후 소급 불가**하므로 v1 계측에 반드시 포함

## 8. QA 핵심 (발췌)

- [ ] 타이머 정확도(서버 remaining vs HUD 오차 ≤2s) · 시계 조작 무효
- [ ] 보상 멱등성: claim 2회 연속 → 1회만 지급
- [ ] 강종/재접속: 타이머·판수 정확 복원
- [ ] 8판 달성·타이머 동시 종료 경합 = 유저 유리(성공)
- [ ] Lv9 이하 이벤트 완전 비노출 / Lv10 클리어 시 인트로 1회
- [ ] ★**팝업 세션 상한 소진 케이스** — 발동 연출이 잘리지 않고 다음 로비 복귀로 이월되는지 · 이월 중 쿨다운 미시작 · `7101` 재발화 없음
- [ ] ★**결과 화면 2단 구성** — 상단 판 보상 RV ×2 정상 동작 / 하단 체스트 [받기] 단일 / 체스트 지급분에 배수 미적용
- [ ] ★**타이머 연장** — 잔여 5분 이하에서만 버튼 노출 · 사이클당 1회 초과 불가(서버 검증) · 시청 중 강종 시 중복 연장 없음
- [ ] ★**실패 토스트** — 다음 로비 복귀 시 1회만 노출 · 팝업 체인 차단 없음 · 사이클당 중복 없음
- [ ] `grace_after_window` — 목 23:59 직전 발동 유저가 금 00:24까지 완주 인정되는지 · 해당 구간 **신규 발동은 없어야 함**
