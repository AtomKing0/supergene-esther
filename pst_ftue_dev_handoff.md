# PST FTUE — 개발 핸드오프

2026-06-17 · 라이브 시트 `1oLb…` · 배경·근거는 기획서 `pst_ftue_analysis.html`
**한 빌드에 동시 배포** (EQR 제출 07/01 전 Toss 개선 업데이트 1회). 시트 파라미터는 클라가 기 참조 → sync로 발효.

**분류 기준:** 없던 변수 추가 = 개발 필요 / 기존 변수 값 변경 = 개발 불필요(단, off값 동작 honor는 확인 필요) / string = 개발 불필요. 모든 항목은 QA 확인 필요.

---

## 0. 한눈에
| 구분 | 일감 | 비고 |
|---|---|---|
| 🟦 클라 구현 (신규 변수) | **R4** · **R5** · **R12** · **팬페이지** | 시트 ✓ / 클라 로직 필요 |
| 🟦 개발 확인 (기존 변수 off) | **R11** | 코드 신설 아님, 클라가 interval=0을 off로 honor하는지 검증 |
| 🟩 QA만 (개발 불필요) | **R3** · **R7** · **R9** · **R10** | string/기존 변수 값변경, 클라 기 참조 |
| 🟦 서버 재량 | **R8** | 원하는 바만 전달, 설계는 서버팀 |
| ⚪ 계측 | — | 현존 이벤트로 충분, 신규 없음 |

---

## 1. 시트 반영 완료 (추가 시트 작업 없음)
| 위치 | 변경 | 일감 | 변수 |
|---|---|---|---|
| `unlock` content_daily_check | 4 → 7 | R7 | 기존 |
| `unlock` content_daily_task | 10 → 13 | R9 | 기존 |
| `const` interstitial_ad_level_clear_interval | 3 → 0 (IT 전체 off) | R11 | 기존 |
| `const` rv_double_unlock_clear_count | = 3 | R4 | **신규** |
| `const` friend_panel_unlock_level | = 15 | R5 | **신규** |
| `const` is_iap_popup_open | = 0 (IAP 자동팝업 off 토글) | R12 | **신규** |
| `const` is_inbox_fanpage_follow_popup_open | = 0 | 팬페이지 | **신규** |
| `string_code` T_TUT_PRE_LEVEL | 전 10로케일 교체 | R3 | string |
| `string_code` T_TUT_BETTING_2 | 전 10로케일(강요 톤 원복) | R10 | string |

> 미변경(의도): content_streak·content_daily_wheel(9). 신규 const는 클라 로직 없으면 무효.

---

## 2. 클라 구현 (신규 변수 → 개발 필요)

**R4 — RV(x2) 노출 게이트**
- 완료화면에서 누적 클리어 < 3 이면 x2 버튼 비노출(받기 단일).
- 선결(P0): 계정 누적 클리어(승리) 수 소스 — 로그엔 판수만, 승리 카운터 없음 → 없으면 신설.

**R5 — 친구 패널 레벨 게이트**
- 레벨 < 15 이면 패널 비노출(팝업 높이 축소, 빈 공간 금지). 잠금 부스터 = 기존 아이콘 그레이스케일 실루엣(신규 아트 없음).
- 선결(P1): 친구 패널이 const/unlock 제어인가 레벨 팝업 하드코딩인가 + 부스터 그레이스케일 가능 여부.

**R12 — IAP 자동팝업 off**
- 신규 const `is_iap_popup_open`=0 (자동팝업 on/off 토글). 클라가 0이면 Starter Pack 자동팝업 미발동. (content_iap_popup=15는 노출 레벨 게이트로 유지, 수동 상점 진입은 유지) — `is_daily_wheel_open` 등과 동일 패턴.

**인박스 팬페이지 팝업**
- const `is_inbox_fanpage_follow_popup_open` 플래그로 인박스 지점 팝업 on/off (0=숨김, 1=노출).

---

## 3. 개발 확인 (기존 변수 off값 — 코드 신설 아님)
> 값만 바꿨지만, 클라가 그 off값을 실제로 honor하는지 확인 필요. 미동작 시 소소한 가드만(신규 변수 아님).

- **R11 — 전면(IT) 광고 off**: `interstitial_ad_level_clear_interval`=0. 시트 설명에 "0=노출 안함" 정의 존재 → 클라가 0을 전체 off로 처리하는지 확인.

---

## 4. 서버 — R8 리더보드 봇 (원하는 바만, 설계는 서버팀 재량)
> 봇 수·점수 분포·성장·닉네임/아바타·리셋 등 구현은 **서버에서 이전 프로젝트 경험으로 알아서**. 아래는 바라는 결과뿐.

**왜:** 초반 리더보드가 비어 있으면 "사람 없는 게임"처럼 보여 퀄·평점↓ (세션 마지막 인상 = 좋아요/싫어요 직결).

**원하는 바:**
- 리더보드가 **초기부터 자연스럽게 채워져** 보일 것 (빈/거의 빈 리스트 금지)
- **같은 세션 안에서 다시 봤을 때 동일한 봇 유저**가 보일 것 (새로고침마다 바뀌지 않게)
- 전반적으로 **자연스럽게** — 신규가 너무 절망적이지 않게 추월 가능한 분포면 좋음
- **랭킹 보상은 봇 제외**, 실유저만 산정. 실유저 늘면 봇 비중 축소

---

## 5. QA 체크 (개발 작업 없음)
| 일감 | 확인 |
|---|---|
| R3 | T_TUT_PRE_LEVEL 카피 주요 로케일 반영 |
| R7 | content_daily_check Lv.7 해금 |
| R9 | content_daily_task Lv.13 해금 |
| R10 | T_TUT_BETTING_2 강요 카피 + 강제 핸드 포인터 노출·베팅 기능 정상 (현행 유지) |

---

## 6. 계측
- **신규 계측 없음.** 현존 이벤트(nth_event × 300초 윈도우 쿼리)로 퍼널·이탈·D1/D7/D14 코호트 측정.
- (선택) `level_index` 1개 — 레벨 단위 분석 대비용. 현 분석엔 불필요.
- 게임 로그 밖: 좋아요 비율(플랫폼·Alex), 변경 발효 확인(QA).

---

## 7. Open Questions (착수 전 선결)
| # | 관련 | 질문 |
|---|---|---|
| P0 | R4 | 계정 누적 클리어(승리) 수 소스 존재 여부 — 없으면 카운터 신설 |
| P0 | 레벨 게이트 | 판수↔레벨 1:1? unlock 출석/미션은 `level_clear`, 친구 패널은 `level` 혼재 |
| P1 | R5 | 친구 패널 제어 방식(const/unlock vs 하드코딩) + 부스터 그레이스케일 |
| P1 | R11·R12 | 클라가 interval=0 / content_iap_popup 고게이트를 off로 honor하는지 |
| P2 | R3 | 교체한 string_code key·전 로케일 커버 확인 |

---

## 8. 데이터 정합
- 06/15 Downloads 스냅샷은 현 라이브와 어긋남(예: iap_popup). **수치 확정은 라이브 시트(1oLb) 우선.**
